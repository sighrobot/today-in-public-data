footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  line-height: 1.5;
  flex-shrink: 0;
  font-size: 10px;
  min-height: 50px;
  margin: 0;
  padding: 0 25px;
}

a {
  color: inherit;
  border-color: white;
  font-size: inherit;
}
