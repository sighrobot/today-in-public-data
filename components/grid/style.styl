.grid {
    margin: 50px 0;
}

.line {
    align-items: center;
    display: flex;
    height: 100px;

    span {
        font-size: 12px;
        flex-shrink: 0;
        text-align: right;
        width: 31px;
    }

    hr {
        background: #ccc;
        border: none;
        height: 1px;
        width: 100%;
    }
}
