import css from 'styled-jsx/css'

export default css`
  .calendar {
    position: relative;
    margin: 0;
    padding: 0;
  }

  .days-wrapper {
    display: flex;
    position: relative;
    z-index:2;
  }
`
